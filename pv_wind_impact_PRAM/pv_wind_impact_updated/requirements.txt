# ---------------------------------------------------------------------------
# Impact Analysis of Atmospheric Parameters on PV & Wind Power Output
# Core dependencies (required)
# ---------------------------------------------------------------------------
streamlit>=1.30
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7

# ---------------------------------------------------------------------------
# Gradient boosting (optional). If xgboost installs, the XGBoost model is added
# automatically; if it (or its native library) is unavailable, the app skips it
# gracefully and still runs with the scikit-learn algorithms.
# ---------------------------------------------------------------------------
xgboost>=2.0

# ---------------------------------------------------------------------------
# Optional dependencies (the app auto-detects these and enables extra
# features only if they are installed — it runs fine without them).
#   lightgbm  -> an additional gradient-boosting algorithm
#   torch     -> enables deep-learning models (CNN, RNN, LSTM, GRU,
#                BiLSTM, Transformer, Autoencoder, RBFN, GAN, DBN,
#                Capsule Network)
#   shap       -> SHAP explainability panel in the Impact tab
# ---------------------------------------------------------------------------
# lightgbm>=4.0
# torch>=2.0
shap>=0.44

# Interactive map location picker (🗺️ Map Location Picker tab)
folium>=0.16
streamlit-folium>=0.20
